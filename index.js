var customerName = 2;

/*
function upperCaseCustomerName() {
   var upperCase = customerName.toUppercase();
    return upperCase;
}

upperCaseCustomerName();

function setBestCustomer() {
    bestCustomer = 'not bob';
    return bestCustomer;
}

setBestCustomer();

function overwriteBestCustomer() {
    var bestCustomer = 'maybe bob';
    return bestCustomer;
}

overwriteBestCustomer();

const leastFavoriteCustomer = 'Jojo Momo';

function changeLeastFavoriteCustomer(leastFavoriteCustomer) {
    const leastFavoriteCustomer = 'Bobby Fischer';
    return leastFavoriteCustomer;
}

changeLeastFavoriteCustomer();